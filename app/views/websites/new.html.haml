.ui.breadcrumb
  %a{:href => websites_path}
    Websites
  %i.right.angle.icon.divider
  .active.section
    Create new website
.ui.divider
%h3.ui.top.attached.header
  Create new website
.ui.attached.segment
  = simple_form_for @website do |f|
    = f.input :name
    = f.input :url, label: 'URL'
    = f.button :submit
