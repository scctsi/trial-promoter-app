- hashtags = false if hashtags.nil?
.ul.list
  - tags.each do |tag|
    - if hashtags
      .item
        .ui.small.basic.label
          = tag
    - else
      .item
        .ui.small.tag.label
          = tag