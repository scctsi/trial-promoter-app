%table.ui.celled.striped.table
  %thead
    %tr
      %th Image
      %th Tags
  %tbody
    - images.each do |image|
      %tr
        %td
          = image_tag "#{image.url}", width: '150', height: '150', lazy: true
        %td= image.tag_list
