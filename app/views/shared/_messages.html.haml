%table.ui.celled.striped.table
  %thead 
    %tr
      %th Content
      %th Platform
      %th
  %tbody
    - messages.each do |message|
      %tr
        %td= message.content
        %td= message.message_template.platform.titleize
        %td
          %a{:href => edit_message_path(message)}
            Edit
